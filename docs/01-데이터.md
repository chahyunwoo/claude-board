# 01. 데이터

이 문서의 모든 값은 2026-09-03 맥북프로에서 **실측**한 것이다.
형식이 바뀌면 여기부터 고친다.

## 소스 A — 살아있는 세션 목록

```bash
claude agents --json
```

```json
[
  {
    "pid": 47559,
    "cwd": "/Users/me/projects/api-server",
    "kind": "interactive",
    "startedAt": 1787835862057,
    "sessionId": "ca9ae832-953a-417a-a10d-590d9dd0f4ba",
    "name": "api-server-f0",
    "status": "busy"
  }
]
```

| 필드 | 비고 |
|---|---|
| `pid` | 살아있음의 근거 |
| `cwd` | 프로젝트 경로 |
| `sessionId` | **소스 B와 잇는 키** |
| `startedAt` | epoch ms |
| `status` | `"busy"` 일 때만 존재. 없으면 유휴로 간주하지 말 것 — 판별은 소스 B로 |

주의: TTY 가 없으면 `claude agents` 는 거부한다. 반드시 `--json` 을 붙인다.

## 소스 B — 세션 기록

```
~/.claude/projects/{경로를-하이픈으로-치환}/{sessionId}.jsonl
```

경로 인코딩 예:
```
/Users/me/projects/notify-service
→ -Users-me-projects-notify-service
```

`sessionId` 로 파일을 직접 찾을 수 있으므로 **인코딩 규칙에 의존하지 말고
`~/.claude/projects/*/{sessionId}.jsonl` 글롭으로 찾는다.** (규칙이 바뀌어도 안전)

### 레코드 타입 분포 (한 세션, 1838줄)

```
assistant              848
user                   498
ai-title               112
last-prompt            112
attachment             110
queue-operation         84
pr-link                 37
file-history-delta      24
file-history-snapshot   13
```

### 쓸 필드

| 필드 | 값 예시 | 용도 |
|---|---|---|
| `aiTitle` | `"결제 실패 재시도 로직 점검"` | 세션 제목 |
| `lastPrompt` | `"재시도 큐가 계속 쌓이기만 하는…"` | 마지막 발화 |
| `gitBranch` | `"feature/47-poller-redesign"` | worktree 브랜치도 그대로 나옴 |
| `permissionMode` | `"auto"` | 권한 모드 |
| `cwd` | 절대 경로 | 소스 A와 대조용 |
| `timestamp` | ISO8601 | 레코드 시각 |
| `message.usage` | 아래 참고 | 컨텍스트 사용량 |

### 컨텍스트 사용량

`assistant` 레코드의 `message.usage` 에서 **가장 마지막 값**을 쓴다.

```json
{
  "input_tokens": 2,
  "cache_creation_input_tokens": 728,
  "cache_read_input_tokens": 454492,
  "output_tokens": 2792
}
```

```
컨텍스트 ≈ input_tokens + cache_read_input_tokens + cache_creation_input_tokens
         = 2 + 454492 + 728 = 455,222
```

실측: `notify-service` 세션이 458,014 토큰이었다.
`output_tokens` 는 합산하지 않는다 (다음 요청의 입력이 되긴 하나 이미 cache_read 에 반영됨).

## 상태 판별 규칙 — 이 도구의 핵심

세션 파일을 **끝에서부터 역순으로** 읽어 첫 `user` 또는 `assistant` 레코드를 찾는다.

```
그 레코드가 assistant                        → 답변 대기
그 레코드가 user 인데 content 에
  tool_result 블록이 있음                    → 작업 중 (도구 결과 반환)
그 레코드가 user 이고 tool_result 아님       → 작업 중 (사용자 입력 직후)
```

**중요**: 도구 결과는 `type: "user"` 로 기록된다.
이걸 사용자 입력으로 오인하면 "작업 중"과 "답변 대기"가 뒤바뀐다.
반드시 `message.content` 배열에 `type: "tool_result"` 블록이 있는지 확인할 것.

```python
content = (rec.get('message') or {}).get('content')
is_tool_result = isinstance(content, list) and any(
    isinstance(b, dict) and b.get('type') == 'tool_result' for b in content)
```

### 최종 상태 5종

| 상태 | 조건 | 정렬 |
|---|---|---|
| `waiting` 답변 대기 | 마지막 = assistant · pid 있음 | 1 |
| `stalled` 멈춤 의심 | 마지막 = tool_result · mtime 이 임계 초과 | 2 |
| `working` 작업 중 | 마지막 = tool_result/user · 최근 갱신 | 3 |
| `idle` 유휴 | pid 있음 · 오래 조용함 | 4 |
| `ended` 종료 | pid 없음 (기록만 존재) | 기본 숨김 |

임계값 초안 — **검증 후 조정할 것**:
```
stalled : 마지막 활동 > 10분 이고 마지막이 tool_result
idle    : 마지막 활동 > 2시간
```

### 검증된 결과 (2026-09-03 16:5x)

```
fb498644  scratch                tool_result  → 작업 중      ← 당시 실행 중이던 세션. 정확
de141b3b  notify-service            assistant    → 답변 대기
59f91106  fix-retry-throttle assistant    → 답변 대기
88022e66  web-client           assistant    → 답변 대기
2dce5a9c  concert-ticketing-...  assistant    → 답변 대기
1e3fde89  api-server    assistant    → 답변 대기
96152f40  docs                   assistant    → 답변 대기
6d4a8903  web-client           assistant    → 답변 대기
ff5c7ed4  notify-service            tool_result  → 작업 중
5654a9cc  web-client           assistant    → 답변 대기
```

## 프로젝트 단위 집계

세션이 아니라 **프로젝트가 1급 단위**다. (한 프로젝트에 세션이 계속 이어짐)

```
프로젝트 키 = cwd
  현재 세션   = 그 프로젝트의 살아있는 세션 중 mtime 최신
  이전 세션들 = 나머지 (접어서 표시)
  세션 순번   = 그 프로젝트 기록 파일을 시각순 정렬했을 때의 index
```

`web-client` 처럼 한 프로젝트에 세션이 여러 개 동시에 살아있을 수 있다.
그 경우 **가장 최근 것을 현재로** 삼고 나머지는 접는다.

## 성능

- 세션 파일 최대 5.8MB, 총 109개
- 전체 파싱 금지. 끝에서 역순으로 읽되 필요한 필드를 다 채우면 즉시 중단
- `aiTitle`·`lastPrompt` 는 파일 뒤쪽에 있을 가능성이 높다 (갱신될 때마다 append)
- 목표: 전체 갱신 **200ms 이내**. [05-검증](05-검증.md) 에서 실측할 것
